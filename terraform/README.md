Terraform examples are minimal and intended for demonstration. Before `terraform apply`:
- Configure AWS credentials (environment variables or profile).
- Review variables.tf and update `ami_id` & `region` as required.
